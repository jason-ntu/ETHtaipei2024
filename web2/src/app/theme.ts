'use client';

import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {main: "#4D7369"},
        secondary: {main: '#FF8C48'},
    },
});

export default theme;